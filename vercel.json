{
  "buildCommand": "cd app && npm install 2>/dev/null || true",
  "outputDirectory": "app",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    },
    {
      "source": "/:path*",
      "destination": "/app/index.html"
    }
  ]
}
